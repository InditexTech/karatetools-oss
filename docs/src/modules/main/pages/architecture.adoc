= Karate Tools architecture
:description: How Karate Tools is built: its modules, and how they fit together.

// TODO(#96 phase 3): draft real content from the old docs/src/modules/overview/
// and docs/src/modules/archetype/ pages and diagrams.

This page describes the modules that make up Karate Tools and how they relate
to each other.

== Overview

// Walk through the module diagram carried over from the old overview/archetype
// pages: each artifact, its purpose, and its dependencies on the others.

== Project structure

// karatetools-oss-archetype, -clients, -generators, -runner, -starter, -boot,
// -openapi-test, -karate-test — one paragraph each, sourced from the old
// contributing/karatetools-oss.adoc anchor sections (see Guides > Archetype).
