# docker-compose environment variables:  <MONGO_INITDB_DATABASE> <MONGO_INITDB_ROOT_USERNAME> <MONGO_INITDB_ROOT_PASSWORD>
# docker-compose folder structure:       NA
hosts: localhost
port: 27017
db-name: KARATE
user: karate
password: karate-pwd
connect-timeout: 10000
server-selection-timeout: 10000
socket-timeout: 10000
