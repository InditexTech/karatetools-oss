# WARNING: test-only dummy credentials used by unit/integration tests. Do not commit real secrets in this file.

# credentials: pairs of username: password. 'auth.password' when used in a specific request.
credentials:
  # username100: Fixed username:password example
  userdev: userpass
